server {
  listen 80;
  server_name default;
  location = / {
    include uwsgi_params;
    uwsgi_modifier1 9;
    uwsgi_pass unix:/var/run/smokeping.socket;
  }
  location /smokeping {
    alias /usr/share/smokeping/www/;
  }
}
